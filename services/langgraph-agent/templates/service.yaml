apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-agent.fullname" . }}
  labels: {{- include "langgraph-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector: {{- include "langgraph-agent.selectorLabels" . | nindent 4 }}
