apiVersion: v1
kind: Service
metadata:
  name: {{ include "linux-mcp-server.fullname" . }}
  labels: {{- include "linux-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
      protocol: TCP
      name: http
  selector: {{- include "linux-mcp-server.selectorLabels" . | nindent 4 }}
