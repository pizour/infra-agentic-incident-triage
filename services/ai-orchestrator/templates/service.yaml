apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-orchestrator.fullname" . }}
  labels:
    {{- include "new-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-orchestrator.selectorLabels" . | nindent 4 }}
