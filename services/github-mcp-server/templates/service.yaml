apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-mcp-server.fullname" . }}
  labels: {{- include "github-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector: {{- include "github-mcp-server.selectorLabels" . | nindent 4 }}
