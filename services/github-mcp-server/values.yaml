replicaCount: 1

image:
  registry: ghcr.io
  project: modelcontextprotocol
  repository: server-github
  name: ""
  pullPolicy: IfNotPresent
  tag: "latest"

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 8080

resources: {}

autoscaling:
  enabled: false

secrets:
  GH_PERSONAL_ACCESS_TOKEN: "" # Loaded via install-secrets pipeline
