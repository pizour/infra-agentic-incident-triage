apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "netbox.fullname" . }}-db
spec:
  serviceName: {{ include "netbox.fullname" . }}-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: netbox-db
  template:
    metadata:
      labels:
        app.kubernetes.io/name: netbox-db
    spec:
      containers:
        - name: db
          image: "{{ .Values.db.image.repository }}:{{ .Values.db.image.tag }}"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.db.user | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.db.database | quote }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "netbox.fullname" . }}-secrets
                  key: NETBOX_DB_PASS
          volumeMounts:
            - name: db-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: db-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        {{- if .Values.db.persistence.storageClass }}
        storageClassName: {{ .Values.db.persistence.storageClass }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.db.persistence.size }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "netbox.fullname" . }}-db
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.db.service.port }}
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/name: netbox-db
