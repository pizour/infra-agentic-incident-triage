apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-fabric.fullname" . }}
  labels:
    {{- include "langgraph-fabric.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "langgraph-fabric.selectorLabels" . | nindent 4 }}
