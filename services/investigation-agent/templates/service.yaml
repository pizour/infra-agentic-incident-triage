apiVersion: v1
kind: Service
metadata:
  name: {{ include "investigation-agent.fullname" . }}
  labels: {{- include "investigation-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector: {{- include "investigation-agent.selectorLabels" . | nindent 4 }}
